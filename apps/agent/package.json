{
  "name": "@pr_hospitalagent/agent",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../.env --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@fastify/cors": "^10",
    "@fastify/jwt": "^10.0.0",
    "@pr_hospitalagent/api-shared": "workspace:*",
    "fastify": "^5",
    "mongodb": "^6",
    "zod": "^3"
  },
  "devDependencies": {
    "@pr_hospitalagent/types": "workspace:*",
    "typescript": "^5"
  }
}
